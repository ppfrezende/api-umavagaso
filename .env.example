NODE_ENV=dev

DATABASE_URL=

# JWT Secret - usado para assinar e verificar tokens JWT
# Gere um secret seguro com: openssl rand -base64 32
JWT_SECRET=

# Email Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@umavagaso.com

# App Configuration
APP_URL=http://localhost:3333
FRONTEND_URL=http://localhost:3000